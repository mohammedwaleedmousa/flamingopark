name: Connect storefront admin

on:
  push:
    branches:
      - agent/connect-storefront-admin

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/connect-storefront-admin
          fetch-depth: 0
      - name: Verify workflow
        run: echo "workflow-ready"
