# تقرير فحص موقع فلامنجو قبل التنفيذ

**تاريخ الفحص:** 3 سبتمبر 2026

**نسخة الأساس:** `3daae571859c563c832ab7cbf2250e2706d95125`

يسجل هذا التقرير حالة الموقع التي بُنيت عليها إصلاحات الميزات 1 و3 و4 و5، مع الحفاظ على تصميم المتجر الحالي ومنطق الأعمال القائم.

## 1. تتبع الطلب

- صفحة التتبع كانت تعتمد عملياً على فتح رابط يحتوي رقم الطلب ورمز التتبع مسبقاً.
- لا توجد واجهة واضحة يستطيع الزائر من خلالها إدخال رقم الطلب ورمز التتبع بعد الرجوع للموقع.
- البنية الخلفية الآمنة موجودة مسبقاً عبر `get_order_tracking`، وهي تتطلب رمز التتبع للضيف وتتحقق من ملكية الطلب للمستخدم المسجل.
- النتيجة: الميزة آمنة من جهة البيانات، لكنها تبدو معطلة للمستخدم الذي يدخل من القائمة أو التذييل.

## 3. الصور والأداء

- بعض صفحات الكتالوج كانت ترسل الصورة الأصلية بلا أحجام متجاوبة أو أبعاد ثابتة.
- مولّد `srcset` كان يعيد كتابة العرض بحسب عرض نافذة المستخدم، ما يجعل أوصاف الأحجام غير مطابقة للصور الناتجة.
- عند فشل رابط صورة محسنة كان `srcset` يبقى نشطاً وقد يعيد محاولة المصدر الفاشل.
- النتيجة: نقل بيانات زائد على الاتصال البطيء واحتمال تحرك التخطيط أثناء تحميل الصور.

## 4. السياسات والثقة

- لا توجد صفحات مستقلة للخصوصية أو الشروط أو الإرجاع أو الشحن.
- التذييل لا يوفّر روابط واضحة لهذه السياسات ولا رابطاً مباشراً لتتبع الطلب.
- صفحة معلومات المتجر تعرض بريداً تجريبياً عند غياب بريد رسمي، وتستخدم ادعاء أصالة مطلقاً لا تدعمه آلية تحقق معروضة للمستخدم.
- النتيجة: نقص في معلومات ما قبل الشراء وعناصر الثقة الأساسية.

## 5. الشحن والدفع

- تمييز شركات الشحن داخل عدن وخارجها كان مبنياً على نص الاسم والوصف، لا على حقل بيانات صريح.
- المحافظة لم تكن مدخلاً إلزامياً مستقلاً، ما يسمح بعنوان ملتبس أو بتكوين شركة شحن لا تناسب الوجهة.
- قيد الدفع عند الاستلام كان موزعاً بين الواجهة وحارس DOM، ويمكن تجاوزه من عميل غير الواجهة.
- لم يكن قبول الشروط والسياسات مطلوباً قبل إنشاء الطلب.
- النتيجة: قابلية لاختيار شحن أو دفع غير متوافق، خصوصاً بين عدن وبقية المحافظات.

## ضوابط التنفيذ

- إبقاء المتجر RTL وتصميمه الوردي/الأبيض الحالي من دون إعادة بناء شاملة.
- عدم فتح جدول الطلبات للزائر؛ استخدام RPC التتبع الآمن القائم فقط.
- فرض قيود الشحن والدفع في قاعدة البيانات بالإضافة إلى الواجهة.
- استخدام هجرة أمامية آمنة ومتوافقة مع السجلات القديمة.
- تحسين الصور تدريجياً مع الرجوع إلى المصدر الأصلي عند تعذر التحويل.
