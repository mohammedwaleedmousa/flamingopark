# تقرير فحص أمان ووظائف صفحات السايدبار

التاريخ: 2026-08-16  
النطاق: الصفحات التي يستدعيها سايدبار الأدمن أو سايدبار العميل فقط، مع التدفقات اللازمة لإتمام وظائفها مثل إنشاء الطلب. لم تُفحص الصفحات المخفية الأخرى فحصاً وظيفياً كاملاً.

## الخلاصة التنفيذية

- تم استخراج 35 مساراً يستدعيها سايدبار الأدمن و13 مساراً يستدعيها سايدبار العميل، وجميعها تملك Routes فعلية داخل التطبيق.
- تم تحويل دخول العميل إلى رقم جوال يمني موحّد بصيغة `+9677XXXXXXXX` مع رمز سري رقمي من 6 إلى 12 رقماً، ومنع الرموز الضعيفة والمتسلسلة والمتكررة.
- أصبحت جلسة العميل معتمدة على Supabase Auth والتحقق من المستخدم لدى الخادم؛ بيانات `localStorage` مجرد Cache وليست دليلاً على الهوية.
- تم نقل تسعير الطلب وإنشائه إلى الخادم، وربط الطلبات والفواتير والعناوين والمفضلة والإشعارات بهوية Auth الموثقة.
- تم تضييق سياسات RLS والتخزين وصلاحيات دوال `SECURITY DEFINER`، وإزالة رموز التتبع النصية من قاعدة البيانات بعد تحويلها إلى Hash.
- لم تُطبق أي Migration أو Edge Function على مشروع الإنتاج أثناء الفحص. التغييرات موجودة في مسودة Pull Request وتحتاج اختبار Staging قبل الدمج والنشر.

## تغطية صفحات الأدمن

| المجموعة | المسارات المستدعاة من السايدبار | نتيجة الوظائف | نتيجة الأمان |
|---|---|---|---|
| الدخول | `/admin/login` | Route وبناء ناجحان | التحقق عبر `getUser()` ثم دور الأدمن؛ أُلغي تجاوز `?dev=true` |
| الرئيسية | `/admin`، `/admin/orders`، `/admin/products`، `/admin/customers` | Routes موجودة، واستعلامات الصفحات تُبنى بنجاح | الطلبات والعملاء محميون بالملكية/دور الأدمن؛ تكلفة المنتج تحتاج معالجة مستقلة |
| الإجراء السريع | `/admin/products/new` | Route موجود والنموذج داخل حزمة الإنتاج | حفظ المنتج محمي بسياسة الأدمن؛ قراءة `cost_price` ما زالت نقطة مفتوحة |
| الكتالوج | `/admin/categories`، `/admin/brands`، `/admin/brand-category-map`، `/admin/inventory-adjustments` | Routes وحزمة الإنتاج ناجحة | عمليات الكتابة تعتمد دور الأدمن؛ تكلفة المخزون مكشوفة حالياً لدور المتصفح |
| واجهة المتجر | `/admin/banners`، `/admin/sections`، `/admin/content`، `/admin/brand-pages`، `/admin/brand-sections`، `/admin/brand-filters` | جميع Routes موجودة | تم تضييق رفع الملفات للأدمن وصور العميل لمساره فقط |
| العمليات | `/admin/delivery`، `/admin/cod-regions`، `/admin/reviews` | جميع Routes موجودة | التوصيل وCOD للأدمن؛ إدخال التقييم العام يحتاج Endpoint محدود المعدل |
| المالية | `/admin/invoices`، `/admin/payment-methods`، `/admin/expenses`، `/admin/ledger`، `/admin/refunds`، `/admin/currencies` | جميع Routes موجودة | الفواتير خاصة بروابط موقعة، ودوال المالية الإدارية مُراجعة ومقيّدة؛ تقرير الربح يعتمد تكلفة المنتج المفتوحة |
| التسويق | `/admin/campaigns`، `/admin/offers`، `/admin/coupons`، `/admin/customer-notifications`، `/admin/notification-deliveries` | جميع Routes موجودة | القسائم والإشعارات الإدارية مقيّدة؛ سياسة `notification_deliveries` القديمة تحتاج تصحيح هوية العميل |
| التقارير | `/admin/reports`، `/admin/reports/finance`، `/admin/reports/customers` | جميع Routes موجودة | القراءة للأدمن؛ استقبال أحداث Analytics مباشرة من المتصفح يحتاج وسيط خادم ومحدد معدل |
| النظام | `/admin/settings`، `/admin/audit-log` | Routes موجودة | الكتابة للأدمن؛ مفاتيح `site_settings` الحالية محتوى متجر عام ولا تتضمن أسراراً حسب فحص أسماء المفاتيح |

## تغطية صفحات العميل

| المجموعة | المسارات المستدعاة من السايدبار | نتيجة الوظائف | نتيجة الأمان |
|---|---|---|---|
| الدخول | `/auth` | رقم يمني + رمز سري، تسجيل ودخول وضيف | Auth حقيقي، Rate limit لتهيئة الحساب، منع الرموز الضعيفة، لا توجد كلمة مرور أو Hash في `public` |
| التسوق | `/home`، `/categories`، `/products`، `/seasonal-offers`، `/new-arrivals`، `/best-sellers` | Routes موجودة والبناء ناجح | القراءة العامة مقصودة؛ حقل تكلفة المنتج يحتاج عزله عن عميل REST |
| السلة | `/cart` | Route موجود؛ تدفق Checkout المرتبط به يبني بنجاح | السعر والخصم والتوصيل والقسيمة يعاد حسابها في الخادم ولا تُقبل إجماليات العميل |
| الحساب | `/account` | تحميل وتحديث الملف والعناوين والطلبات والفواتير مربوط بالمستخدم | الهاتف والهوية غير قابلين للتبديل من العميل، وصورة الحساب محصورة في مسار المستخدم ونوع/حجم محددين |
| المفضلة | `/favorites` | Route ومزامنة موجودان | Trigger وRLS يربطان السجل تلقائياً بـ `auth.uid()` |
| الإشعارات | `/notifications` | Route موجود وإظهار غير المقروء يعمل | قراءة إشعارات المستخدم والبث فقط؛ تعديل العميل محصور بحالة القراءة |
| معلومات المتجر | `/store-info`، `/qr-code` | Routes موجودة | محتوى عام مقصود ولا يحتوي مفاتيح سرية بحسب المفاتيح الموجودة حالياً |

حالة الضيف لم تعد تُعامل كحساب مسجل في السايدبار؛ يظهر له زر «تسجيل الدخول» بدلاً من «تسجيل الخروج».

## الإصلاحات الأمنية المنفذة في مسودة التغيير

1. **هوية العميل**
   - استخدام Supabase Auth برقم الهاتف والرمز السري.
   - منع التسجيل المجهول والعام خارج وظيفة التهيئة المحمية.
   - ترحيل حسابات العملاء القديمة بعد التحقق من صيغة الهاتف وHash من نوع bcrypt، مع نقل بيانات الاعتماد إلى Schema خاص.
   - ربط صف `customers` بـ `user_id` فريد والتحقق من الجلسة عبر الخادم.

2. **الطلبات والتتبع**
   - وظيفة `create-order` لا تقبل سعر المنتج أو الإجماليات أو تكلفة التوصيل من المتصفح.
   - الخادم يعيد جلب المنتجات والإضافات والتوصيل والقسائم وأسعار العملات ويحسب Snapshot الطلب.
   - محدد معدل حسب بصمة الطلب ورقم الهاتف، وحد لحجم Body وعدد العناصر وأطوال النصوص.
   - مالك الطلب والأدمن يقرآن الطلب من خلال الهوية؛ الضيف يحتاج رمز تتبع عشوائياً ويُخزن Hash فقط.

3. **الفواتير والملفات**
   - Bucket الفواتير خاص، والتحميل PDF فقط حتى 6MB مع فحص توقيع الملف.
   - الوصول عبر رابط موقّع قصير المدة بعد تحقق المالك/الأدمن/رمز الضيف.
   - Bucket الرفع محدد الحجم والأنواع، وصورة العميل محصورة بمساره.

4. **قاعدة البيانات**
   - سياسات RLS للطلبات والعملاء والعناوين والمفضلة والإشعارات والأرشيف.
   - تقييد تنفيذ دوال `SECURITY DEFINER` افتراضياً ثم إعادة منح الدوال التي تمت مراجعتها فقط.
   - إزالة `TRUNCATE` و`REFERENCES` و`TRIGGER` من أدوار المتصفح.
   - حماية الحقول الثابتة للملف والإشعارات بواسطة Triggers.

## نقاط مفتوحة مرتبة بالأولوية

| الأولوية | النقطة | الأثر | العلاج المقترح |
|---|---|---|---|
| P1 مرتفعة | `products.cost_price` قابل للقراءة بسبب صلاحية SELECT العامة | كشف هامش الربح وتكلفة البضاعة | نقل التكلفة إلى جدول خاص أو RPC إداري، ثم إزالة الحقل من استعلامات المتجر ومنح الأعمدة العامة فقط |
| P1 مرتفعة | إدخال `analytics_events` مباشر للعامة | تلويث التقارير أو تضخيم قاعدة البيانات | Edge Function تتحقق من الأنواع والأحجام وتطبق Rate limit، ثم سحب INSERT من `anon/authenticated` |
| P1 مرتفعة | إدخال `product_reviews` مباشر للعامة | Spam أو انتحال اسم عميل | Endpoint لإرسال التقييم، ربط المستخدم إن كان مسجلاً، CAPTCHA/Rate limit ومراجعة قبل النشر |
| P2 متوسطة | سياسة العميل القديمة في `notification_deliveries` تقارن معرف العميل بـ Auth UID | نتائج تسليم العميل قد لا تظهر مستقبلاً | المقارنة عبر `customers.user_id` أو إضافة `user_id` صريح |
| P2 متوسطة | حماية كلمات المرور المسربة غير مفعلة في Supabase Auth | يهم حسابات الأدمن ذات كلمة المرور | تفعيل Leaked Password Protection وفرض كلمة قوية وMFA للأدمن |
| P2 متوسطة | لا توجد آلية استرداد رمز العميل موثقة | العميل قد يفقد الوصول | اعتماد استرداد يدوي موثق أو OTP عندما تتوفر قناة رسائل موثوقة في اليمن |
| P3 أداء | بعض حزم الصور/PDF كبيرة | تحميل أبطأ على شبكات الهاتف | فصل الأدوات الثقيلة عند الطلب وتحسين Code splitting |

الصفحات غير الموجودة في السايدبار، باستثناء التدفقات اللازمة للطلب، مؤجلة حسب طلب صاحب المشروع.

## التحقق المنفذ

- `npm run build`: ناجح، 5432 module تم تحويلها.
- `npm run test:security`: ناجح، 8 من 8 اختبارات.
- `deno check` لوظائف Edge الثلاث: ناجح.
- تحليل Migration: 145 عبارة SQL قابلة للتحليل.
- `git diff --check`: ناجح.
- ESLint للملفات المعدلة في دفعة السايدبار: دون أخطاء أو تحذيرات.
- فحص قاعدة الإنتاج كان للقراءة فقط؛ لم تُغير بيانات أو إعدادات أو Functions.

## بوابة النشر

لا يُنصح بدمج التغيير أو تشغيل `supabase db push` مباشرة على الإنتاج بسبب وجود Drift بين تاريخ Migrations في المستودع والإنتاج. الترتيب الآمن:

1. نسخة احتياطية وقاعدة Staging/فرع Supabase.
2. تطبيق Migration الجديدة وفحص Advisors.
3. نشر `customer-auth-bootstrap` ثم `create-order` ثم `invoice-access` بالإعدادات الموجودة في `config.toml`.
4. تشغيل سيناريوهات دخول عميل قديم وجديد، ضيف، إنشاء طلب، تتبع، حساب، مفضلة، إشعار وفاتورة.
5. نشر واجهة الويب، ثم مراقبة Auth/API/Edge logs قبل الدمج النهائي.
