# Sales agent codes

Each social-media employee can receive a unique code such as `FP-101`. When a customer enters that code during checkout, the order is attributed to the employee while preserving the customer's eligible discount.

The admin page is available at `/admin/sales-agents` and shows active codes, order counts, and attributed sales.
