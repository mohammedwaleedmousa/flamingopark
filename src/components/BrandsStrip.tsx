import { useLayoutEffect, useMemo, useRef } from "react";
import { Link } from "react-router-dom";
import { useQuery } from "@tanstack/react-query";
import { ArrowLeft } from "lucide-react";
import { supabase } from "@/integrations/supabase/client";
import { optimizeImage } from "@/lib/imageUrl";
interface BrandRow { id:string; name:string; slug:string|null; logo_url:string|null; countries:string[]|null; is_active:boolean|null; sort_order:number|null; }
interface BrandViewModel { id:string; name:string; slug:string; logo_url:string|null; }
const BRAND_STRIP_POSITION_KEY="flamingo-home-brand-strip-position";
const BrandsStrip=({enabled=true}:{enabled?:boolean})=>{const scrollerRef=useRef<HTMLDivElement|null>(null);const{data:brands=[]}=useQuery({queryKey:["home-brands"],enabled,queryFn:async()=>{const{data,error}=await supabase.from("brands").select("id,name,logo_url,countries,is_active,sort_order,slug").eq("is_active",true).order("sort_order",{ascending:true});if(error)throw error;return(data||[]) as BrandRow[];},staleTime:600000,refetchOnWindowFocus:false});const renderBrands=useMemo<BrandViewModel[]>(()=>brands.map(b=>({id:b.id,name:b.name,slug:b.slug||b.name.toLowerCase().trim().replace(/\s+/g,"-"),logo_url:b.logo_url})),[brands]);useLayoutEffect(()=>{if(!enabled||!renderBrands.length||!scrollerRef.current)return;try{const saved=sessionStorage.getItem(BRAND_STRIP_POSITION_KEY);if(saved){const p=JSON.parse(saved);if(typeof p.scrollLeft==="number")scrollerRef.current.scrollLeft=p.scrollLeft;}}catch{}},[enabled,renderBrands.length]);const saveStripPosition=(id:string)=>{if(!scrollerRef.current)return;try{sessionStorage.setItem(BRAND_STRIP_POSITION_KEY,JSON.stringify({scrollLeft:scrollerRef.current.scrollLeft,brandId:id}));}catch{}};if(!enabled||!renderBrands.length)return null;return <section className="w-full overflow-hidden border-y border-[#eee5e1] bg-white py-5 md:py-10" dir="rtl" aria-label="الماركات"><div className="mx-auto w-full max-w-[1600px] px-3 md:px-8 lg:px-12"><div className="mb-3 flex items-end justify-between md:mb-6"><div><div className="mb-1 flex items-center gap-2"><span className="h-[2px] w-4 bg-[#D4777D] md:w-7"/><span className="font-serif text-[6px] tracking-[.2em] text-[#B86168] md:text-[9px]">BRANDS</span></div><h2 className="text-[16px] font-semibold md:text-[28px]">أشهر الماركات</h2></div><Link to="/brands" className="flex items-center gap-2 text-[7px] font-medium text-[#A95B61] md:text-[10px]">جميع الماركات<ArrowLeft className="h-4 w-4"/></Link></div><div ref={scrollerRef} className="-mx-3 overflow-x-auto px-3 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden md:mx-0 md:overflow-visible md:px-0"><div className="flex w-max gap-2.5 md:grid md:w-full md:grid-cols-8 md:gap-0 md:overflow-hidden md:rounded-[18px] md:border md:border-[#eee6e2]">{renderBrands.slice(0,8).map((brand,i)=><Link key={brand.id} to={`/brands/${brand.slug}`} onClick={()=>saveStripPosition(brand.id)} className={`group flex w-[78px] shrink-0 flex-col items-center md:w-auto md:border-l md:border-[#eee6e2] md:px-3 md:py-5 ${i===7?"md:border-l-0":""}`}><div className="flex aspect-square w-full items-center justify-center rounded-[15px] border border-border/60 bg-white px-2.5 md:h-[62px] md:aspect-auto md:border-0 md:p-0">{brand.logo_url?<img src={optimizeImage(brand.logo_url,240,78)} alt={brand.name} loading="lazy" decoding="async" className="max-h-[45%] max-w-[82%] object-contain transition-transform duration-300 group-hover:scale-105 md:max-h-[38px]"/>:<span className="font-serif text-[10px] font-semibold md:text-[15px]">{brand.name}</span>}</div><p className="mt-1.5 truncate text-[8px] font-semibold md:mt-2 md:text-[9px] md:text-[#74635e]">{brand.name}</p></Link>)}</div></div></div></section>};export default BrandsStrip;
